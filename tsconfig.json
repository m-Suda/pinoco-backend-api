{
  "compilerOptions": {
    "outDir": "./dist",
    "module": "commonjs",
    "target": "es6",
    "sourceMap": true,
    // Any型の使用を許可する（一部のimportができない場合を考慮して。）,
    "noImplicitAny": false,
    // コンパイルに含めるライブラリファイルの指定,
    "lib": [
      "es2018",
      "dom"
    ],
    // モジュールの名前解決の方法 https://www.typescriptlang.org/docs/handbook/module-resolution.html,
    "moduleResolution": "node",
    // /*!で始まるコメント以外を除去する,
    "removeComments": true,
    // noImplicitAny, noImplicitThis, alwaysStrict, strictNullChecks を true にする,
    "strict": true,
    // 使ってないローカル変数をエラーにする,
    "noUnusedLocals": false,
    // 使ってない仮引数をエラーにする,
    "noUnusedParameters": false,
    // 暗黙的に undefined を返却している関数をエラーにする,
    "noImplicitReturns": true
  },
  "exclude": [
    "node_modules"
  ],
  "include": [
    "./src/**/*"
  ]
}